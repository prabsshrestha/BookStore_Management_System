@{
    var role = ViewData["Role"]?.ToString();
}

<h1>Welcome @role!</h1>

@if (role == "Admin")
{
    <p>You have admin privileges.</p>
    <button>Admin Action</button>
}
else
{
    <p>You are a customer.</p>
    <button>Customer Action</button>
}
